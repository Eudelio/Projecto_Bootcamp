from django import forms

class formulario
class Formulario (forms.from):
    nombre = forms.CharFielt(max_length=100)
    telefono = forms.IntegerField()
    ciudad = forms.CharFielt(max_length=20)
    servicios = forms.CharFielt(max_length=500)
    breve_descripcion = forms.TextField(max_length=500)
